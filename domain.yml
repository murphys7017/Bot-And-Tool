version: "3.1"

intents:
  - get_setu
  - out_of_scope

actions:
  - action_get_setu
  - action_fallback
responses:
  utter_default:
  - text: "PASS"


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
